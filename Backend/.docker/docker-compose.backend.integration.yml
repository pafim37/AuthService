name: auth-service-integration-tests
services:
  database:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "AuthServiceDevelopmentPassword123!" 
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P \"$${MSSQL_SA_PASSWORD}\" -C -Q \"SELECT 1\" || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 5s

  backend:
    build:
      context: ../src
      dockerfile: ../.docker/auth-server.Dockerfile
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
      ASPNETCORE_URLS: "http://+:5124"
      ConnectionStrings__Default: "Server=database,1433;Database=AuthServiceDatabase;User Id=sa;Password=AuthServiceDevelopmentPassword123!;TrustServerCertificate=True;Encrypt=True;"
      Jwt__Issuer: "AuthServerService"
      Jwt__Audience: "AdministratorPanel"
      Jwt__AuthServiceKey: "DevelopmentOnlyAuthServiceKeyThatShouldBeReplacedWithASecretValue12345"
    ports:
      - "5124:5124"
    depends_on:
      database:
        condition: service_healthy
